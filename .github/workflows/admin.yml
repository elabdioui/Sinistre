name: Admin Service CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'Admin/**'
  workflow_dispatch:

jobs:
  build-and-push:
    name: Build Admin Service
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      
      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      
      - name: Build and push
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        run: |
          cd service-admin
          docker build -t $ECR_REGISTRY/sinistre/admin:latest .
          docker push $ECR_REGISTRY/sinistre/admin:latest
          echo "✅ Admin image pushed!"
